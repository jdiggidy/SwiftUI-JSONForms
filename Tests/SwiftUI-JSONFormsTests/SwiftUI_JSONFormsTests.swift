import XCTest
@testable import SwiftUI_JSONForms

final class SwiftUI_JSONFormsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUI_JSONForms().text, "Hello, World!")
    }
}
